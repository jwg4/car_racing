language: python
python:
  - "2.7"
install:
- pip install -r test_requirements.txt
script:
  - python -m unittest discover
  - flake8 . --max-complexity 5 --max-line-length 99

branches:
  only:
    - master

notifications:
  email: false
